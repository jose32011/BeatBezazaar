# Render Environment Variables Template
# Copy these to your Render web service environment variables

# Database Connection (REQUIRED)
# Get this from your Render PostgreSQL service - use INTERNAL URL
DATABASE_URL=postgresql://beatbazaar_user:password@dpg-xxxxx-a:5432/beatbazaar

# Session Security (REQUIRED)
# Generate a long, random secret key
SESSION_SECRET=your-super-secret-session-key-here-make-it-long-and-random-at-least-32-characters

# Node Environment (REQUIRED)
NODE_ENV=production

# Email Settings (OPTIONAL - for password reset, notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# File Upload Settings (OPTIONAL)
MAX_FILE_SIZE=50000000

# Custom Domain (OPTIONAL)
DOMAIN=yourdomain.com

# Instructions:
# 1. Create PostgreSQL database on Render
# 2. Create web service on Render
# 3. Copy the INTERNAL database URL (not external)
# 4. Add these variables to your web service environment
# 5. Deploy your app
# 6. Run migrations if needed
# 7. Create admin user via /setup page